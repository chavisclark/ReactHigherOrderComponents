# Highlighting Higher Order Components

###Getting Started###

Checkout this repo, install dependencies, then start the gulp process with the following:

```
	> git clone git@github.com:chavisclark/ReactHigherOrderComponents.git hoc
	> cd hoc
	> npm install
	> npm start
```
